<Page x:Class="posk.Pages.Menu.PageGaugeTest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 

      xmlns:local="clr-namespace:posk.Pages.Menu"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="PageGaugeTest">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <!--
        <lvc:AngularGauge Grid.Row="1" Value="{Binding V}" FromValue="0" ToValue="{Binding Z}" 
                                  LabelsStep="100000" TicksStep="50000" Wedge="300"
                                  TicksForeground="White" Foreground="White"
                                  FontWeight="Bold" FontSize="12"
                                  SectionsInnerRadius=".5">
            <lvc:AngularGauge.Sections>
                <lvc:AngularSection FromValue="0" ToValue="{Binding A}" Fill="#FF151515"/>
                <lvc:AngularSection FromValue="{Binding A}" ToValue="{Binding Z}" Fill="#FF198B52"/>
            </lvc:AngularGauge.Sections>
        </lvc:AngularGauge>
        -->
    </Grid>
</Page>
