<UserControl x:Class="posk.Controls.Accion.ItemEnviar_pedido"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:posk.Controls.Accion"
             mc:Ignorable="d" 
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto">
    <Button x:Name="btnEnviar" Style="{StaticResource BtnClear}" Background="{x:Null}" Height="58" FontWeight="Regular">
        <Grid Margin="16 0 0 0" Width="152">
            <!--
            <Border Panel.ZIndex="1" Background="#4C000000">
                <Label Content="PAGAR" Panel.ZIndex="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Foreground="White" FontSize="12"/>
            </Border>
            -->
            <Button Height="58" FontSize="26" Background="#FF06704D" Style="{StaticResource BtnClear}" Margin="0 0 0 0">
                <materialDesign:PackIcon Kind="Send" Height="48" Width="48" Foreground="#fafafa" VerticalContentAlignment="Center"/>
            </Button>
        </Grid>
    </Button>
</UserControl>
