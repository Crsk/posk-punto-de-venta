<UserControl x:Class="posk.Controls.ItemEscoger"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:posk.Controls"
             mc:Ignorable="d" 
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto">
    <Grid Width="100" Margin="12 4 12 4">
        <Button Grid.Column="1" x:Name="btnOpcion" Background="#FF0A7562" Height="36" Style="{StaticResource BtnClear}">
            <Grid>
                <Border HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0 0 0 0">
                    <TextBlock x:Name="txtNombre" Grid.Column="1" Text="OPCIÓN" TextWrapping="Wrap" Foreground="#FFDDE0E2" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="4 0 4 0"/>
                </Border>
            </Grid>
        </Button>
    </Grid>
</UserControl>
